// Power calculator using ** operator
function calculatePower(base, exponent) {
    return base ** exponent;
  }
  
  
  const base = 2;
  const exponent = 3;
  const result = calculatePower(base, exponent);
  console.log(`${base} to the power of ${exponent} is ${result}`); 